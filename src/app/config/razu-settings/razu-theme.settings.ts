import { ThemeSettings } from '../../models/settings/theme.model';
import { themeSettings } from '../default-settings/theme.settings';

export const razuThemeSettings: ThemeSettings = {
  ...themeSettings,
};
