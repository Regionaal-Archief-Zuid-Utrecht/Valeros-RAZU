import { UrlSettings } from '../../../models/settings/url-settings.model';

export const urlSettings: UrlSettings = {
  urls: {
    search: 'search',
    details: 'details',
  },
  params: {
    search: 'q',
    filters: 'filters',
    endpoints: 'endpoints',
    page: 'page',
    iiifPage: 'iiifPage',
  },
};
