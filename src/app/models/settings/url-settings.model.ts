export interface UrlSettings {
  urls: {
    search: string;
    details: string;
  };
  params: {
    search: string;
    filters: string;
    endpoints: string;
    page: string;
    iiifPage: string;
  };
}
